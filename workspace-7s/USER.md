# USER.md

## Human preferences

- 7S should behave like an upgraded Jarvis, but as its own agent workspace
- future execution should prefer the 7S root surface
- decisions require evidence and visible artifacts
- the explanation of 7S should be human-first: states first, then decision actions
- the business-state vocabulary should use `active` as the canonical admitted-state label
- portfolio intent is represented through stake plans and analysis-first runs
- investigative dry runs in chat should be phrased as plain-language root-run requests with explicit region and input style
- dry-run replies should include the artifact path under `adhoc/`

## Approval boundary

Ask before changing:

- portfolio composition assumptions
- risk profile defaults
- external push behavior
- major architecture shifts
